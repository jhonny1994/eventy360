{
  "tasks": [
    {
      "id": "0285b53f-57e1-439d-b538-6e6c3eab187b",
      "name": "Setup Supabase Browser Client & Auth Provider",
      "description": "Create a Supabase client instance specifically for browser usage and set up a React Context provider to manage and distribute authentication state (session, user) throughout the application.",
      "status": "已完成",
      "dependencies": [],
      "createdAt": "2025-05-05T13:26:42.894Z",
      "updatedAt": "2025-05-05T13:34:01.220Z",
      "relatedFiles": [
        {
          "path": "src/lib/supabase/client.ts",
          "type": "CREATE",
          "description": "Supabase browser client instance"
        },
        {
          "path": "src/components/providers/AuthProvider.tsx",
          "type": "CREATE",
          "description": "React Context provider for auth state"
        },
        {
          "path": "src/app/[locale]/layout.tsx",
          "type": "TO_MODIFY",
          "description": "Wrap layout with AuthProvider"
        }
      ],
      "implementationGuide": "1. Create `src/lib/supabase/client.ts`. Initialize Supabase client using `createBrowserClient` with public env vars.\n2. Create `src/components/providers/AuthProvider.tsx`. Use React Context API (`createContext`, `useContext`).\n3. Manage session state using Supabase `onAuthStateChange`. Provide `session`, `user`, `loading` state via context.\n4. Wrap the root layout (`src/app/[locale]/layout.tsx`) with this provider.",
      "verificationCriteria": "Auth state (session, user, loading) is accessible via a hook (`useAuth`) in client components. Session updates correctly on login/logout.",
      "analysisResult": "1.  **Supabase Client Setup**: Need a browser client instance. Can be placed in a utility file (e.g., `src/lib/supabase/client.ts`).\n2.  **Auth Context**: A React Context (`src/components/providers/AuthProvider.tsx`?) is suitable for managing session/user state globally, making it accessible via a custom hook (e.g., `useAuth`).\n3.  **Auth UI**: Login (`src/app/[locale]/login/page.tsx`) and Register (`src/app/[locale]/register/page.tsx`) pages with dedicated form components (`src/app/[locale]/ui/auth/LoginForm.tsx`, `RegisterForm.tsx`). Use Flowbite, RHF, Zod. Need clear loading (button disable/spinner) and error display (Toast/Alert).\n4.  **Route Protection**: Update existing `middleware.ts`. Check for session on protected routes (e.g., `/profile`, `/dashboard`). Redirect to `/login` if no session.\n5.  **Profile Data Fetching**: A client-side approach is appropriate. Use SWR or TanStack Query for caching and state management. Fetch from `profiles`, `researcher_profiles`, `organizer_profiles` based on user ID from auth context. Implement in a custom hook (`src/hooks/useUserProfile.ts`?).\n6.  **Profile View**: Component (`src/app/[locale]/ui/profile/ProfileView.tsx`) displaying data from the fetching hook. Handle loading state (skeletons?) and errors.\n7.  **Profile Edit Form**: Component (`src/app/[locale]/ui/profile/ProfileEditForm.tsx`). Use RHF/Zod. Fetch `wilayas` (name_ar) for the first dropdown. Fetch `dairas` (name_ar) dynamically based on selected wilaya. Update `profiles` table via Supabase client call. Handle loading/submission state and errors (Toast).\n8.  **Role-Specific Fields**: Conditionally render input fields within `ProfileEditForm.tsx` based on user type from profile data. Update respective tables (`researcher_profiles`, `organizer_profiles`) in the same update function, potentially using an RPC call if logic becomes complex.\n9.  **UI/RTL**: Ensure `dir=\"rtl\"` is applied correctly (likely in `layout.tsx`). Use Flowbite components which should handle RTL via Tailwind. Test thoroughly.\n10. **Dependencies**: Need `react-hook-form`, `zod`, `@hookform/resolvers` (if not already installed). SWR/TanStack Query if chosen for data fetching.\n11. **Acceptance Criteria**: User can register, login, logout. Profile page is protected. Profile data (common + role-specific + location) is displayed correctly. User can update profile information, including selecting Wilaya/Daira. Loading and error states are shown appropriately. UI is RTL-compliant.",
      "completedAt": "2025-05-05T13:34:01.215Z",
      "summary": "Successfully created the Supabase browser client, AuthProvider context with useAuth hook, and integrated it into the root layout."
    },
    {
      "id": "48dc807e-dcc6-4e50-a8e0-4aedf4e5fd0e",
      "name": "Create Login Page & Form Component",
      "description": "Implement the user login page and its associated form component using Flowbite, React Hook Form, and Zod for validation. Handle Supabase email/password login.",
      "status": "已完成",
      "dependencies": [
        {
          "taskId": "0285b53f-57e1-439d-b538-6e6c3eab187b"
        }
      ],
      "createdAt": "2025-05-05T13:26:42.894Z",
      "updatedAt": "2025-05-05T19:27:51.552Z",
      "relatedFiles": [
        {
          "path": "src/app/[locale]/login/page.tsx",
          "type": "CREATE",
          "description": "Login page route"
        },
        {
          "path": "src/app/[locale]/ui/auth/LoginForm.tsx",
          "type": "CREATE",
          "description": "Login form component"
        }
      ],
      "implementationGuide": "1. Create route `src/app/[locale]/login/page.tsx`.\n2. Create form component `src/app/[locale]/ui/auth/LoginForm.tsx`.\n3. Use Flowbite inputs, button.\n4. Use RHF (`useForm`) and Zod (`zodResolver`) for email/password fields.\n5. On submit, call Supabase `signInWithPassword`. Handle loading state (disable button, show spinner) and errors (display using Toast/Alert). Redirect on success (e.g., to profile page).",
      "verificationCriteria": "User can enter email/password. Validation errors show. User can successfully log in. Loading/error states function correctly. UI uses Flowbite and supports RTL.",
      "analysisResult": "1.  **Supabase Client Setup**: Need a browser client instance. Can be placed in a utility file (e.g., `src/lib/supabase/client.ts`).\n2.  **Auth Context**: A React Context (`src/components/providers/AuthProvider.tsx`?) is suitable for managing session/user state globally, making it accessible via a custom hook (e.g., `useAuth`).\n3.  **Auth UI**: Login (`src/app/[locale]/login/page.tsx`) and Register (`src/app/[locale]/register/page.tsx`) pages with dedicated form components (`src/app/[locale]/ui/auth/LoginForm.tsx`, `RegisterForm.tsx`). Use Flowbite, RHF, Zod. Need clear loading (button disable/spinner) and error display (Toast/Alert).\n4.  **Route Protection**: Update existing `middleware.ts`. Check for session on protected routes (e.g., `/profile`, `/dashboard`). Redirect to `/login` if no session.\n5.  **Profile Data Fetching**: A client-side approach is appropriate. Use SWR or TanStack Query for caching and state management. Fetch from `profiles`, `researcher_profiles`, `organizer_profiles` based on user ID from auth context. Implement in a custom hook (`src/hooks/useUserProfile.ts`?).\n6.  **Profile View**: Component (`src/app/[locale]/ui/profile/ProfileView.tsx`) displaying data from the fetching hook. Handle loading state (skeletons?) and errors.\n7.  **Profile Edit Form**: Component (`src/app/[locale]/ui/profile/ProfileEditForm.tsx`). Use RHF/Zod. Fetch `wilayas` (name_ar) for the first dropdown. Fetch `dairas` (name_ar) dynamically based on selected wilaya. Update `profiles` table via Supabase client call. Handle loading/submission state and errors (Toast).\n8.  **Role-Specific Fields**: Conditionally render input fields within `ProfileEditForm.tsx` based on user type from profile data. Update respective tables (`researcher_profiles`, `organizer_profiles`) in the same update function, potentially using an RPC call if logic becomes complex.\n9.  **UI/RTL**: Ensure `dir=\"rtl\"` is applied correctly (likely in `layout.tsx`). Use Flowbite components which should handle RTL via Tailwind. Test thoroughly.\n10. **Dependencies**: Need `react-hook-form`, `zod`, `@hookform/resolvers` (if not already installed). SWR/TanStack Query if chosen for data fetching.\n11. **Acceptance Criteria**: User can register, login, logout. Profile page is protected. Profile data (common + role-specific + location) is displayed correctly. User can update profile information, including selecting Wilaya/Daira. Loading and error states are shown appropriately. UI is RTL-compliant.",
      "summary": "Created login page and form component using Flowbite, RHF, Zod. Implemented Supabase login call, loading state, toast notifications for success/error, and basic i18n/RTL support via hooks and component library.",
      "completedAt": "2025-05-05T19:27:51.551Z"
    },
    {
      "id": "cec629f6-b58d-4f34-bc88-7e1fe298b2b4",
      "name": "Create Registration Page & Form Component",
      "description": "Implement the user registration page and form using Flowbite, RHF, and Zod. Handle Supabase email/password sign-up.",
      "status": "已完成",
      "dependencies": [
        {
          "taskId": "0285b53f-57e1-439d-b538-6e6c3eab187b"
        }
      ],
      "createdAt": "2025-05-05T13:26:42.894Z",
      "updatedAt": "2025-05-06T07:11:27.861Z",
      "relatedFiles": [
        {
          "path": "src/app/[locale]/register/page.tsx",
          "type": "CREATE",
          "description": "Registration page route"
        },
        {
          "path": "src/app/[locale]/ui/auth/RegisterForm.tsx",
          "type": "CREATE",
          "description": "Registration form component"
        }
      ],
      "implementationGuide": "1. Create route `src/app/[locale]/register/page.tsx`.\n2. Create form component `src/app/[locale]/ui/auth/RegisterForm.tsx`.\n3. Use Flowbite inputs (email, password, confirm password), button.\n4. Use RHF/Zod for validation (ensure passwords match).\n5. On submit, call Supabase `signUp`. Handle loading state and errors (Toast/Alert). Show success message (e.g., \"Check email for verification\").",
      "verificationCriteria": "User can enter registration details. Validation errors show (including password mismatch). User can successfully register. Loading/error/success states function correctly. UI uses Flowbite and supports RTL.",
      "analysisResult": "1.  **Supabase Client Setup**: Need a browser client instance. Can be placed in a utility file (e.g., `src/lib/supabase/client.ts`).\n2.  **Auth Context**: A React Context (`src/components/providers/AuthProvider.tsx`?) is suitable for managing session/user state globally, making it accessible via a custom hook (e.g., `useAuth`).\n3.  **Auth UI**: Login (`src/app/[locale]/login/page.tsx`) and Register (`src/app/[locale]/register/page.tsx`) pages with dedicated form components (`src/app/[locale]/ui/auth/LoginForm.tsx`, `RegisterForm.tsx`). Use Flowbite, RHF, Zod. Need clear loading (button disable/spinner) and error display (Toast/Alert).\n4.  **Route Protection**: Update existing `middleware.ts`. Check for session on protected routes (e.g., `/profile`, `/dashboard`). Redirect to `/login` if no session.\n5.  **Profile Data Fetching**: A client-side approach is appropriate. Use SWR or TanStack Query for caching and state management. Fetch from `profiles`, `researcher_profiles`, `organizer_profiles` based on user ID from auth context. Implement in a custom hook (`src/hooks/useUserProfile.ts`?).\n6.  **Profile View**: Component (`src/app/[locale]/ui/profile/ProfileView.tsx`) displaying data from the fetching hook. Handle loading state (skeletons?) and errors.\n7.  **Profile Edit Form**: Component (`src/app/[locale]/ui/profile/ProfileEditForm.tsx`). Use RHF/Zod. Fetch `wilayas` (name_ar) for the first dropdown. Fetch `dairas` (name_ar) dynamically based on selected wilaya. Update `profiles` table via Supabase client call. Handle loading/submission state and errors (Toast).\n8.  **Role-Specific Fields**: Conditionally render input fields within `ProfileEditForm.tsx` based on user type from profile data. Update respective tables (`researcher_profiles`, `organizer_profiles`) in the same update function, potentially using an RPC call if logic becomes complex.\n9.  **UI/RTL**: Ensure `dir=\"rtl\"` is applied correctly (likely in `layout.tsx`). Use Flowbite components which should handle RTL via Tailwind. Test thoroughly.\n10. **Dependencies**: Need `react-hook-form`, `zod`, `@hookform/resolvers` (if not already installed). SWR/TanStack Query if chosen for data fetching.\n11. **Acceptance Criteria**: User can register, login, logout. Profile page is protected. Profile data (common + role-specific + location) is displayed correctly. User can update profile information, including selecting Wilaya/Daira. Loading and error states are shown appropriately. UI is RTL-compliant.",
      "summary": "Registration page and form created successfully. Includes email, password, confirmation, and user type selection (Researcher/Organizer). Uses RHF/Zod with internationalized validation messages. Calls Supabase signUp correctly, passing user type. Handles loading, errors (inline/Alert/Toast), and success states (clears form, shows message/Toast). UI uses Flowbite components, two-column layout, and password visibility toggles. Fully internationalized (UI strings, Zod messages) for ar/en/fr.",
      "completedAt": "2025-05-06T07:11:27.860Z"
    },
    {
      "id": "fdbbe239-2ac6-483d-87f8-fa34e816d4d2",
      "name": "Implement Route Protection Middleware",
      "description": "Update the Next.js middleware to protect specific routes (e.g., profile) by redirecting unauthenticated users to the login page.",
      "status": "已完成",
      "dependencies": [
        {
          "taskId": "48dc807e-dcc6-4e50-a8e0-4aedf4e5fd0e"
        }
      ],
      "createdAt": "2025-05-05T13:26:42.894Z",
      "updatedAt": "2025-05-05T20:00:50.404Z",
      "relatedFiles": [
        {
          "path": "middleware.ts",
          "type": "TO_MODIFY",
          "description": "Add auth checks for protected routes"
        }
      ],
      "implementationGuide": "1. Open `middleware.ts`.\n2. Use Supabase `createMiddlewareClient`.\n3. Get session using `supabase.auth.getSession()`.\n4. Define protected routes (e.g., `/profile`, `/dashboard`).\n5. Check if the current request path matches a protected route and if `session` is null.\n6. If protected and no session, redirect to `/login` (`NextResponse.redirect`).",
      "verificationCriteria": "Unauthenticated users accessing `/profile` are redirected to `/login`. Authenticated users can access `/profile`.",
      "analysisResult": "1.  **Supabase Client Setup**: Need a browser client instance. Can be placed in a utility file (e.g., `src/lib/supabase/client.ts`).\n2.  **Auth Context**: A React Context (`src/components/providers/AuthProvider.tsx`?) is suitable for managing session/user state globally, making it accessible via a custom hook (e.g., `useAuth`).\n3.  **Auth UI**: Login (`src/app/[locale]/login/page.tsx`) and Register (`src/app/[locale]/register/page.tsx`) pages with dedicated form components (`src/app/[locale]/ui/auth/LoginForm.tsx`, `RegisterForm.tsx`). Use Flowbite, RHF, Zod. Need clear loading (button disable/spinner) and error display (Toast/Alert).\n4.  **Route Protection**: Update existing `middleware.ts`. Check for session on protected routes (e.g., `/profile`, `/dashboard`). Redirect to `/login` if no session.\n5.  **Profile Data Fetching**: A client-side approach is appropriate. Use SWR or TanStack Query for caching and state management. Fetch from `profiles`, `researcher_profiles`, `organizer_profiles` based on user ID from auth context. Implement in a custom hook (`src/hooks/useUserProfile.ts`?).\n6.  **Profile View**: Component (`src/app/[locale]/ui/profile/ProfileView.tsx`) displaying data from the fetching hook. Handle loading state (skeletons?) and errors.\n7.  **Profile Edit Form**: Component (`src/app/[locale]/ui/profile/ProfileEditForm.tsx`). Use RHF/Zod. Fetch `wilayas` (name_ar) for the first dropdown. Fetch `dairas` (name_ar) dynamically based on selected wilaya. Update `profiles` table via Supabase client call. Handle loading/submission state and errors (Toast).\n8.  **Role-Specific Fields**: Conditionally render input fields within `ProfileEditForm.tsx` based on user type from profile data. Update respective tables (`researcher_profiles`, `organizer_profiles`) in the same update function, potentially using an RPC call if logic becomes complex.\n9.  **UI/RTL**: Ensure `dir=\"rtl\"` is applied correctly (likely in `layout.tsx`). Use Flowbite components which should handle RTL via Tailwind. Test thoroughly.\n10. **Dependencies**: Need `react-hook-form`, `zod`, `@hookform/resolvers` (if not already installed). SWR/TanStack Query if chosen for data fetching.\n11. **Acceptance Criteria**: User can register, login, logout. Profile page is protected. Profile data (common + role-specific + location) is displayed correctly. User can update profile information, including selecting Wilaya/Daira. Loading and error states are shown appropriately. UI is RTL-compliant.",
      "summary": "Verified that the existing middleware.ts correctly implements route protection logic: defines public paths, checks user session, and redirects unauthenticated users from protected routes to /login and authenticated users from /login or /register to /profile.",
      "completedAt": "2025-05-05T20:00:50.401Z"
    },
    {
      "id": "c8a7cbc3-54f3-4d53-8f62-be623747a542",
      "name": "Create Profile Data Fetching Hook",
      "description": "Implement a custom React hook to fetch the authenticated user's profile data (from `profiles`, `researcher_profiles`, `organizer_profiles`) using the Supabase client.",
      "status": "已完成",
      "dependencies": [
        {
          "taskId": "0285b53f-57e1-439d-b538-6e6c3eab187b"
        }
      ],
      "createdAt": "2025-05-05T13:26:42.894Z",
      "updatedAt": "2025-05-07T14:13:57.783Z",
      "relatedFiles": [
        {
          "path": "src/hooks/useUserProfile.ts",
          "type": "CREATE",
          "description": "Custom hook for fetching user profile data"
        },
        {
          "path": "src/lib/supabase/client.ts",
          "type": "REFERENCE",
          "description": "Supabase client needed for fetching"
        },
        {
          "path": "src/components/providers/AuthProvider.tsx",
          "type": "REFERENCE",
          "description": "Provides user ID"
        }
      ],
      "implementationGuide": "1. Create `src/hooks/useUserProfile.ts`.\n2. Use the `useAuth` hook to get the user ID.\n3. Use `useEffect` and Supabase client (`client.ts`) OR integrate SWR/TanStack Query.\n4. Fetch data from `profiles` table where `id` matches user ID. Include related data from `researcher_profiles` or `organizer_profiles` via joins or separate queries.\n5. Return profile data, loading state, and error state.",
      "verificationCriteria": "Hook returns correct profile data for the logged-in user, including role-specific details. Loading and error states are accurate.",
      "analysisResult": "1.  **Supabase Client Setup**: Need a browser client instance. Can be placed in a utility file (e.g., `src/lib/supabase/client.ts`).\n2.  **Auth Context**: A React Context (`src/components/providers/AuthProvider.tsx`?) is suitable for managing session/user state globally, making it accessible via a custom hook (e.g., `useAuth`).\n3.  **Auth UI**: Login (`src/app/[locale]/login/page.tsx`) and Register (`src/app/[locale]/register/page.tsx`) pages with dedicated form components (`src/app/[locale]/ui/auth/LoginForm.tsx`, `RegisterForm.tsx`). Use Flowbite, RHF, Zod. Need clear loading (button disable/spinner) and error display (Toast/Alert).\n4.  **Route Protection**: Update existing `middleware.ts`. Check for session on protected routes (e.g., `/profile`, `/dashboard`). Redirect to `/login` if no session.\n5.  **Profile Data Fetching**: A client-side approach is appropriate. Use SWR or TanStack Query for caching and state management. Fetch from `profiles`, `researcher_profiles`, `organizer_profiles` based on user ID from auth context. Implement in a custom hook (`src/hooks/useUserProfile.ts`?).\n6.  **Profile View**: Component (`src/app/[locale]/ui/profile/ProfileView.tsx`) displaying data from the fetching hook. Handle loading state (skeletons?) and errors.\n7.  **Profile Edit Form**: Component (`src/app/[locale]/ui/profile/ProfileEditForm.tsx`). Use RHF/Zod. Fetch `wilayas` (name_ar) for the first dropdown. Fetch `dairas` (name_ar) dynamically based on selected wilaya. Update `profiles` table via Supabase client call. Handle loading/submission state and errors (Toast).\n8.  **Role-Specific Fields**: Conditionally render input fields within `ProfileEditForm.tsx` based on user type from profile data. Update respective tables (`researcher_profiles`, `organizer_profiles`) in the same update function, potentially using an RPC call if logic becomes complex.\n9.  **UI/RTL**: Ensure `dir=\"rtl\"` is applied correctly (likely in `layout.tsx`). Use Flowbite components which should handle RTL via Tailwind. Test thoroughly.\n10. **Dependencies**: Need `react-hook-form`, `zod`, `@hookform/resolvers` (if not already installed). SWR/TanStack Query if chosen for data fetching.\n11. **Acceptance Criteria**: User can register, login, logout. Profile page is protected. Profile data (common + role-specific + location) is displayed correctly. User can update profile information, including selecting Wilaya/Daira. Loading and error states are shown appropriately. UI is RTL-compliant.",
      "summary": "Successfully implemented the useUserProfile hook that efficiently fetches authenticated user profile data from both the base profiles table and the role-specific extension tables (researcher_profiles, organizer_profiles, admin_profiles). The hook properly handles loading states, error states, and different user types. It exports comprehensive TypeScript types for the profile data structures, making the hook type-safe and easy to use in components.",
      "completedAt": "2025-05-07T14:13:57.782Z"
    }
  ]
}